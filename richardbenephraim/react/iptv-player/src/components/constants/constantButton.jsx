

const ConstantButton = ({text}) => {
    return (
        <div>
            <button  className="bg-blue-400 rounded border border-white/50 backdrop-blur-md">{text}</button>
        </div>
    )
};

export default ConstantButton;